<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Task</title>
    <style>
      body {
        margin: 60px;
        background-color: aliceblue;
      }

      h1 {
        color: chocolate;
        font-size: 30px;
      }

      #title {
        height: 100px;
        width: 50%;
        margin-bottom: 100px;
        padding: 10px;
      }

      textarea:focus {
        outline-color: chocolate;
      }

      #create {
        height: 50px;
        width: 50%;
        border-radius: 16px;
        color: white;
        border: none;
        background-color: chocolate;
      }

      .error {
        color: red;
        font-size: 16px;
        font-weight: bold;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <h1>Create a new task</h1>
    <form action="/views/tasks/create" method="post">
      <label for="title">What to do:</label><br /><br />
      <textarea name="title" id="title" cols="30" rows="10"></textarea>
      <input type="hidden" value="Pending" name="status" />
      <input type="hidden" value="<%= user._id %>" name="user_id" />
      <br />
      <input type="submit" id="create" value="Create task" />
    </form>
    <% if (error) { %>
    <p class="error"><%= error %></p>
    <% } %>
  </body>
</html>
